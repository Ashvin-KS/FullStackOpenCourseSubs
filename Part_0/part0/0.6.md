
```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes in the text field and clicks the save button.

    Note right of browser: The JavaScript code creates a new note object and adds it to the notes list on the page immediately. It then sends the note to the server.

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note right of browser: The new note is sent as a JSON string. The browser does NOT navigate to a new page.

    activate server
    server-->>browser: HTTP 201 Created (The server responds with a success status)
    deactivate server
    ```